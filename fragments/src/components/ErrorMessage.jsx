function ErrorMessage({ Items }) {
    //let foodItems = [];
    if (foodItems.length === 0) {
        return (<p>
            I am still hungry

        </p>
        );
    }
    return null;
}

export default ErrorMessage;